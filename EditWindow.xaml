<Window x:Class="CallBook.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CallBook"
        mc:Ignorable="d"
        Title="EditWindow" Height="216" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#FFEFEFF2" PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="79*"/>
            <RowDefinition Height="17*"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Name="editPanel">
                        

        </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button x:Name="OKbutton" Content="_OK"   VerticalAlignment="Top" Width="75" Click="OKbutton_Click"/>
            <StackPanel Width="5" />
            <Button x:Name="Cancelbutton" Content="_Cancel"  VerticalAlignment="Top" Width="75" Click="Cancelbutton_Click"/>
        </StackPanel>

    </Grid>
</Window>
